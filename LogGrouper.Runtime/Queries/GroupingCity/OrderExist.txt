SELECT O.REFERENCEDOCUMENT AS [ORDER], SUSR3, SUSR4, O.EXTERNORDERKEY, O.C_ADDRESS4, O.ORDERKEY, O.[TYPE]
FROM {Scheme}.ORDERS O
WHERE ORDERKEY in (SELECT DISTINCT ORDERKEY FROM {Scheme}.PICKDETAIL WHERE DROPID = '{Lpn}')
AND O.STORERKEY = '{Storerkey}'